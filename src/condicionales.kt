fun main(args: Array<String>){
    var numero : Int=10
    //uso basico del if
    if (numero == 0)
        println("El valor del numero es 0")
    else
        println("El numero $numero es distinto de cero")

    var a=1
    var b=2
    var max=a
    if(a>b)
        max=a
    else
        max=b
}